# ZMK Configuration for nice!nano 2x5 Macropad
# 双模：USB + BLE

CONFIG_ZMK_USB=y

CONFIG_ZMK_BLE=y

# 30 分钟无操作后进入低功耗睡眠
CONFIG_ZMK_SLEEP=y
CONFIG_ZMK_IDLE_SLEEP_TIMEOUT=1800000

# 临时调试：通过 USB CDC 输出 ZMK 日志（诊断蓝牙问题用，修好后移除）
CONFIG_ZMK_USB_LOGGING=y
